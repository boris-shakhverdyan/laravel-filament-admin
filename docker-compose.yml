mysql:
    image: mysql:8
    restart: unless-stopped
    ports:
        - "3306:3306"
    environment:
        MYSQL_DATABASE: app
        MYSQL_ROOT_PASSWORD: root
        MYSQL_PASSWORD: root
    volumes:
        - mysql_data:/var/lib/mysql

volumes:
    mysql_data:
